{
  "installCommand": "npm install",
  "buildCommand": "npm run build --workspace=shared && npm run prisma:generate --workspace=server && npm run build --workspace=client",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
